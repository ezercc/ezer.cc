---

interface Props {
  items: { title: string; text: string }[];
}

const { items } = Astro.props;
---

<div class="sy-statements">
    {
      items.map(({ title, text }, i) => (
        <div class="box">
          <div class="u-container">
            <div class="content">
              <h2
                class="u-heading"
                data-sy-reveal="words"
              >
                {title}
              </h2>
              <p class="u-text" data-sy-reveal={i === 0 ? "fade" : undefined}>
                {text}
              </p>
            </div>
          </div>
        </div>
      ))
    }
  <div data-gl-place="0"></div>
</div>

<style>
  [data-gl-place] {
    --d: 1;
    --s: 17rem;
    position: absolute;
    right: 0;
    left: 0;
    margin: 0 auto;
    bottom: 0;

    @media (--mq-tablet) {
      bottom: 50%;
    }
  }

  .sy-statements {
    margin-bottom: 0;
    position: relative;
    z-index: 9;
  }

  .box {
    width: 100%;
    height: 100vh;
    display: flex;
    align-items: center;
    justify-content: center;
    background: var(--color-bg);
    color: var(--color-text);
    text-align: center;
    flex-direction: column;

    &:nth-child(2) {
      background: var(--color-brand-1);
      color: var(--color-brand-3);
    }

    &:nth-child(3) {
      background: var(--color-brand-3);
    }
  }

  .content {
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    gap: 1.5rem;
  }

  h2 {
    --fs: 3rem;
    --lh: 1.3;
  }

  p {
    max-width: 40em;
  }
</style>
